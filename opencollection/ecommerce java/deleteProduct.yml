info:
  name: deleteProduct
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/products/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
