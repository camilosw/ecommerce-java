info:
  name: addProduct
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/products
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "sku": "123",
        "price": 10,
        "stockQuantity": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
